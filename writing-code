#ettiquite:
#all indentations are four spaces
#prioritize readability
#put spaces around operators


#adding context:
#a # comments out the rest of the line

"""This is a doc string and will come up if someone hovers over a method name. It provides greater context than just commenting out a line.
Subheadings:
    The doc trinng allows sub headings and paragraphs but the indentation is in line with the opeining quotes.
It begins and ends with triple quotes."""

'''This is also a doc string. First line is about 80 character quick synopsis
The first line ends in a period
Subsequent lines are a summary.
The triple quotes can be singles or doubles.'''


"""variables
variable_names use snake_case

types of variable contents: integer, float, string, boolean

converting variable types
    int() #convert to integer (will round down, boolean becomes 1=True 0=False)
    float() #convert to float (decimal)
    str() #convert to string
    bool() #convert to boolean (data present = True, 0 or empty = False)
    dict[,]

formatted strings allow combining different types in one print statement or variable (for printing)
    f"This string contains a {variable} or {number}." 

type annotations, labels what a variable should accept as values, makes large code faster. Do in establishment variable_name: typeAnnotation = value

self-assigning - allows tracking of changes over time
    variable = "value"
    variable = variable + 1
    can also be written:
    variable = "value"
    variable += 1
"""


"""operators
== equality operator, is equal to
!= inequality operator, is not equall to
>= greater than or equal to
<= less than or equal to
+ addition (will add numbers or print strings one after another)
    print("2"+"3") > 23 
    or print(2+3) > 5 
    but print(2+"3") > error. must use f"{2}+3"
- subtraction (only works on int and float)
* multiplication (only works on int and float)
/ division (only works on int and float)"""
and - used in if statements, ifel statements, and while loops (only runs if both conditions are true)
or - used in if statements, ifel statements, and while loops (only runs if a condition is true)
booleans accept/output:   True   and   False
+= self-assignment with addition (strings or numbers)
-= self-assignment with subtraction (strings or numbers)
*= self-assignment with multiplication (numbers only)
/= self-assignment with division (numbers only, will atomatically change int to float)
"""


#functions
print() #displays content in console. Must be same types or in formatted string.

print(type(variable_name)) #shows what type a variable is: <class 'bool'>

input() 
#gets input from the user, can show the user a string prompt (remember to end with a blank space for readability). 
#store it into a variable to reuse this info later.
user_input=input("What is your name? ")


#if statements
if True:
    print("This code block runs when condition met.")
    print("Multpile lines must be equally indented.")
print ("Non-indented lines always run. The end statement has already closed.")
if True:
    print("This code block runs when condition met.")

if False:
    print("This code block gets skipped when the condition is true.")

if variable_name != input_value:
    print("Not Quite")
if variable_name == input_value:
    print("Great work!")

if not variable_name:
    print(f"This is not {variable_name}.")

if condition_one:
    print("The condition has been met.")
else:
    print("The condition was not met in this if/else statement.")

if condition_one:
    print("First condition met")
ifel condition_two:
    print("First condition not met, second condition met.")
else:
    print("No conditions met.")


#loops
#infinite loops will crash your code. Always close a loop with a condition change or counter.

condition_met = True
while condition_met:
    print("This only goes once.")
    condition_met = False

counter = 1
while counter < 6:
    print("Gonna see this 5 times.")
    counter += 1

for i in range(4)
    print("Getting four lines of this.")

variable("more ")
for i in range(3)
    print(variable)
"""that last for loop outputs:
    more 
    more more
    more more more """


